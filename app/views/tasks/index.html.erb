<h1>TASK一覧</h1>

<ul>
  <% @tasks.each do |task| %> 
    <li><%= link_to task.id, task %> : TASK: <%= task.content %>, STATUS: <%= task.status %></li>
  <% end %>
</ul>
<%= link_to '新規TASKの登録', new_task_path %>